<dispatch_started id="{{xml:id}}">
Task: {{xml:task}}
Transcripts: {{xml:transcriptDir}}

The `dispatch` is running in the background; its result will arrive automatically. Briefly acknowledge it, then end the turn. Do not duplicate the delegated work, speculate about its outcome, or inspect its transcript unless the user asks.
</dispatch_started>
