<dispatch_started id="{{xml:id}}">
Task: {{xml:task}}
Transcripts: {{xml:transcriptDir}}

The `dispatch` is running in the background; the result will arrive automatically. If your next step depends on the result, stop here; do not redo the delegated task or answer based on its expected result. Do not monitor, retrieve, or inspect transcripts unless the user asks.
</dispatch_started>
